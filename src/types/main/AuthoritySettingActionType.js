import {createAction} from 'redux-actions';

export const setUserGroupList = createAction('SET_USER_GROUP_LIST');
export const setMenuList = createAction('SET_MENU_LIST');
export const setCurrentRemark = createAction('SET_CURRENT_USER_GROUP_REMARK');